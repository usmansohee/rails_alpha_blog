<h3 class="mt-3"> All Bloggers </h3>
<hr class="my-4">



<% @user.each do |user| %>
  <div class="card bg-light mt-2">
    <div class="card-body">
      <h6>Name: <%= link_to user.username, user_path(user) %></h6>
      <h6>Email: <%= user.email %></h6>
      <hr class="my-4">
      <p>Total Articles: <%=  user.articles.count %></p>
      <hr class="my-4">
      <%= link_to "Edit this user", edit_user_path(user), class: "btn btn-secondary btn-sm" %></td>
      <td> <%= button_to "Delete this user", article_path(user), method: :delete , class: "btn btn-sm btn-danger btn-sm" %>
    </div>
  </div>
<%end %>


